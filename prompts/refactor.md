整理以下 .gitignore 文件。

【当前内容】
```
%s
```

要求：
1. 移除重复规则，只保留首次出现
2. 按类别重新分组，每组用注释标题分隔（如 # === 依赖 ===、# === 构建产物 ===、# === IDE ===、# === 系统文件 ===）
3. 不要新增任何规则，不要删除任何规则
4. 保留所有注释，合理归类
5. 输出仅为整理后的 .gitignore 原始文本，不要包含 markdown 代码块或解释